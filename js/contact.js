/* =========================================================
   MEDICAL PREMIUM
   CONTACT
   ========================================================= */

document.addEventListener("DOMContentLoaded", () => {

  const whatsappButton = document.querySelector(".mp-whatsapp");

  if (!whatsappButton) return;

  /*
   * El enlace de WhatsApp se maneja directamente
   * mediante el href definido en index.html.
   *
   * No usamos preventDefault().
   * No redirigimos a #contacto.
   */

});